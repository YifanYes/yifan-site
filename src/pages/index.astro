---
import { DEFAULT_LOCALE, getLocalizedPath } from '../lib/i18n';

const redirectPath = getLocalizedPath(DEFAULT_LOCALE);

if (!redirectPath) {
	throw new Error('Missing localized redirect path.');
}

return Astro.redirect(redirectPath, 302);
---
